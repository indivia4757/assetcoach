# AssetCoach 디자인 시스템 (Design System)

> **단계**: 4단계 — 디자인 시스템 정식화
> **작성일**: 2026-04-28
> **입력 산출물**: IA(`AssetCoach_information_architecture.md`) · 세그먼트 매트릭스(`AssetCoach_segment_matrix.md`) · 쇼케이스 HTML(`AssetCoach_showcase.html`)
> **목적**: 5단계 와이어프레임의 빌딩 블록 정의 + 세그먼트별 변주 규칙 명문화

---

## 🎯 디자인 원칙

1. **고요한 깊이 (Quiet Depth)** — 화려한 그라데이션·과한 모션 대신, 종이 질감과 절제된 색으로 신뢰를 만든다. 금융 앱은 차분해야 한다.
2. **읽히는 숫자 (Readable Numbers)** — 모든 금액은 한눈에 들어온다. 천 단위 구분, 단위 명시(원·%·배), 폰트 크기와 무게로 위계 강조.
3. **첫 1초 개인화 (Personalized in 1 Second)** — 화면 진입 후 0.5초 안에 "내 화면"임을 체감. 세그먼트별 컴포넌트 변주는 시각적 일관성을 깨지 않는 선에서.
4. **전 연령 접근성 (Accessible Across Ages)** — 19세부터 80세까지. 은퇴 모드는 별도가 아니라 같은 시스템의 한 모드.
5. **대화가 일등 시민 (Chat is First-Class)** — Gemma 4 대화 UI는 곁다리가 아니라 핵심 컴포넌트. 채팅 버블은 카드와 동급의 디자인 비중.
6. **숫자보다 서사 (Narrative over Numbers)** — 모든 데이터에는 한 문장의 해석이 따라붙는다. 차트 단독 노출 금지.

---

## 🎨 컬러 시스템

### 1. 코어 팔레트 (Core)

| 토큰 | 값 | 역할 | 사용 빈도 |
|---|---|---|---|
| `--cream` | `#F7F3EC` | 배경 (라이트 기본) | 60% |
| `--cream-deep` | `#EFE8DA` | 배경 강조 (섹션 분리) | 10% |
| `--paper` | `#FAF6EE` | 카드 배경 | 15% |
| `--forest` | `#1C2A1E` | 본문·헤딩 텍스트 | 25% |
| `--forest-soft` | `#2E3F30` | 보조 텍스트 | 8% |
| `--terracotta` | `#C66A4A` | 강조·CTA·중요 알림 | 5% |
| `--terracotta-deep` | `#A85536` | 누름 상태·경고 | 1% |
| `--mustard` | `#D4AF6A` | 보조 강조·하이라이트 | 4% |
| `--mustard-deep` | `#B8934B` | 호버·서브 액센트 | 1% |
| `--ink` | `#14201B` | 가장 강한 텍스트 (제목 정점) | 2% |
| `--highlight` | `#F0E5CE` | 형광펜 효과 (인사이트 강조) | 3% |

### 2. 시맨틱 팔레트 (Semantic)

| 토큰 | 값 | 의미 |
|---|---|---|
| `--positive` | `#5C7A4F` | 절약·증가(자산)·긍정 |
| `--positive-bg` | `#E8EFE0` | 긍정 카드 배경 |
| `--negative` | `#A8543E` | 초과·감소(자산)·주의 (terracotta 계열로 톤 통일) |
| `--negative-bg` | `#F4E1D8` | 주의 카드 배경 |
| `--neutral` | `#8B8478` | 평균·중간 |
| `--info` | `#5B6B7A` | 정보·중립 안내 |

> ⚠️ **빨강·파랑 금지**. 토스·뱅샐의 신호등 컬러 회피. 절약 = 숲의 초록, 주의 = 테라코타로 통일하여 차분한 톤 유지.

### 3. 다크 모드 (Night Edition)

| 토큰 | 라이트 | 다크 |
|---|---|---|
| 배경 기본 | `#F7F3EC` | `#1A1F1B` |
| 배경 카드 | `#FAF6EE` | `#252B26` |
| 본문 | `#1C2A1E` | `#E8E2D4` |
| 강조 | `#C66A4A` | `#D88566` (채도 ↓, 명도 ↑) |
| 머스타드 | `#D4AF6A` | `#E0BC7C` |

### 4. 컬러 사용 규칙

- **한 화면에 강조색은 최대 2종** — 테라코타 + 머스타드까지. 동시에 같이 쓰면 매거진 표지 효과.
- **포레스트 그린은 배경으로 사용 자제** — 다크 모드 외에는 텍스트·아이콘·구분선에만.
- **그라데이션 금지** — 단색 + 종이 질감 노이즈 오버레이만 허용.
- **컬러는 카테고리에 종속되지 않는다** — 18개 카테고리 차트는 세리프 라벨 + 무채색 그라데이션으로 구분 (컬러 코드 없음). 인지 부하 감소.

---

## 🔤 타이포그래피

### 1. 폰트 패밀리

| 토큰 | 폰트 | 용도 |
|---|---|---|
| `--serif-kr` | **Gowun Batang** (700/400) | 제목·서사 헤더·인용 (감정·따뜻함) |
| `--serif-kr-alt` | Noto Serif KR (300~900) | 긴 본문이 필요한 리포트 |
| `--sans-kr` | **Pretendard Variable** | 본문·UI 라벨·숫자·버튼 (가독성·실용) |
| `--serif-en` | Fraunces (가변) | 영문 제목·매거진 헤더 |
| `--mono` | JetBrains Mono | 코드·금액 정렬·로그 |

### 2. 타입 스케일 (모바일 기준 / 단위: px)

| 토큰 | 크기 | 행간 | 자간 | 폰트 | 용도 |
|---|---|---|---|---|---|
| `--t-display` | 48 / 56 (은퇴) | 1.05 | -0.02em | Gowun Batang 700 | 온보딩 첫 화면 |
| `--t-h1` | 32 / 40 | 1.15 | -0.015em | Gowun Batang 700 | 화면 제목 |
| `--t-h2` | 24 / 30 | 1.2 | -0.01em | Gowun Batang 700 | 섹션 제목 |
| `--t-narrative` | 20 / 26 | 1.4 | -0.005em | Gowun Batang 400 | 홈 서사 헤더 (Block 1) |
| `--t-body-lg` | 17 / 22 | 1.55 | 0 | Pretendard 400 | 카드 본문 (강조) |
| `--t-body` | 15 / 20 | 1.55 | 0 | Pretendard 400 | 일반 본문 |
| `--t-body-sm` | 13 / 18 | 1.5 | 0.005em | Pretendard 400 | 메타·보조 |
| `--t-caption` | 11 / 14 | 1.4 | 0.02em | Pretendard 500 | 라벨·태그 |
| `--t-num-xl` | 36 | 1.0 | -0.025em | Pretendard 700 | 게이지 정점 숫자 |
| `--t-num-lg` | 24 | 1.1 | -0.015em | Pretendard 600 | 카드 핵심 숫자 |
| `--t-num` | 17 | 1.3 | -0.005em | Pretendard 600 | 거래 금액 |

### 3. 은퇴 모드 (S14·S17 등 L5-L6)

자동으로 모든 타입 크기 **+20%**, 행간 **+10%**, 자간 0으로 리셋. 단순한 글씨일수록 편하게.

```
일반: --t-body = 15px / 1.55
은퇴: --t-body = 18px / 1.7
```

### 4. 숫자 표기 규칙

- **천 단위 구분**: 항상 `,` 사용 (`1,234,567원`)
- **만/억 절삭**: 100만 이상은 한국식 단위 (`1.2억원`, `12만원`)
- **퍼센트 vs 퍼센트포인트**: `12%`(비율), `2%p`(차이) 명확 구분
- **음수**: 빨강 X — 테라코타 + 마이너스 기호 (`−12,000원`, en-dash 사용)
- **소수점**: 게이지 숫자만 1자리, 거래/잔액은 정수

---

## 📐 스페이싱 & 그리드

### 1. 8pt 그리드

| 토큰 | 값 |
|---|---|
| `--s-1` | 4px |
| `--s-2` | 8px |
| `--s-3` | 12px |
| `--s-4` | 16px |
| `--s-5` | 24px |
| `--s-6` | 32px |
| `--s-7` | 48px |
| `--s-8` | 64px |
| `--s-9` | 96px |

### 2. 화면 레이아웃 (모바일)

- 화면 좌우 패딩: `--s-5` (24px) 기본 / 은퇴 모드 `--s-4` (16px, 글씨가 커지므로 패딩 줄임)
- 카드 내부 패딩: `--s-5` (24px) 사방
- 카드 사이 간격: `--s-3` (12px)
- 섹션 사이 간격: `--s-6` (32px)
- 탭바 높이: 56 + safe area / 은퇴 모드 64

### 3. 라운딩

| 토큰 | 값 | 사용처 |
|---|---|---|
| `--r-1` | 4px | 칩·태그 |
| `--r-2` | 8px | 입력 필드·작은 버튼 |
| `--r-3` | 12px | 일반 카드 |
| `--r-4` | 16px | 큰 카드·시트 |
| `--r-5` | 24px | 모달·대화 버블 |
| `--r-pill` | 999px | 알약 버튼·필터 |

### 4. 종이 질감 오버레이

모든 화면에 **노이즈 오버레이** 적용 (쇼케이스 HTML과 동일 SVG 필터). `opacity: 0.18`, `mix-blend-mode: multiply`. 배터리 영향 최소화 위해 1회 SVG 텍스처 캐싱.

---

## 🎬 모션 시스템

### 1. 이징 토큰

| 토큰 | 값 | 성격 |
|---|---|---|
| `--ease-out-paper` | `cubic-bezier(0.2, 0.8, 0.2, 1)` | 종이가 천천히 안착 (기본) |
| `--ease-in-paper` | `cubic-bezier(0.6, 0, 0.8, 0.2)` | 빠르게 사라짐 |
| `--ease-spring` | `cubic-bezier(0.34, 1.56, 0.64, 1)` | 게이지 채움·축하 |
| `--ease-linear` | `linear` | 진행 표시기 |

### 2. 지속시간

| 토큰 | 값 | 사용 |
|---|---|---|
| `--d-quick` | 120ms | 호버·누름 |
| `--d-base` | 240ms | 기본 트랜지션 |
| `--d-slow` | 360ms | 화면 전환 |
| `--d-narrative` | 600ms | 서사 헤더 페이드인 |
| `--d-gauge` | 900ms | 게이지·차트 채움 |

### 3. 표준 모션 패턴

- **카드 진입**: `translateY(8px)` + opacity 0→1, 240ms `--ease-out-paper`, 120ms 시차로 카드 순차 등장
- **숫자 카운트업**: 게이지 숫자는 0 → 목표값으로 900ms 보간 (단, 거래 목록 금액은 카운트업 없음 — 산만함)
- **AI 응답 타이핑**: Gemma 4 응답은 한 글자씩 타이핑 효과 (실제 토큰 스트리밍과 동기)
- **페이지 전환**: iOS 푸시 / Android Material — 플랫폼 네이티브 따름
- **축소 모션 사용자**: `prefers-reduced-motion` 감지 시 모든 모션 제거 (페이드만 유지)

### 4. 금기

- 바운스·튕김 (금융 앱 톤 깨짐)
- 무한 루프 애니메이션 (배터리·인지 부하)
- 1초 초과 트랜지션 (게이지 채움 외)

---

## 🧩 컴포넌트 라이브러리

### 1. 버튼 (Button)

| 변형 | 배경 | 텍스트 | 라운딩 | 용도 |
|---|---|---|---|---|
| **Primary** | `--terracotta` | `--cream` | `--r-3` | 주요 행동 (시작·저장) |
| **Secondary** | 투명 + 1.5px `--forest` 테두리 | `--forest` | `--r-3` | 부차 행동 |
| **Tertiary** | 투명 | `--forest` (밑줄) | 0 | 인라인 텍스트 액션 |
| **Destructive** | 투명 + `--terracotta-deep` 테두리 | `--terracotta-deep` | `--r-3` | 삭제·해지 |
| **Pill Filter** | `--cream-deep` | `--forest` | `--r-pill` | 필터·기간 선택 |

크기: `lg` 56h / `md` 48h (기본) / `sm` 40h. 은퇴 모드는 모두 +8px.

### 2. 카드 (Card)

```
┌─────────────────────────────────────┐
│ [Eyebrow 라벨]                      │  ← --t-caption, mustard-deep
│                                     │
│ 핵심 숫자 또는 제목                 │  ← --t-num-lg or --t-h2
│ 한 줄 해석 서사                     │  ← --t-body, narrative
│                                     │
│ [선택: 차트·게이지·진행바]          │
│                                     │
│ [액션 → 또는 더보기]                │  ← Tertiary button
└─────────────────────────────────────┘
```

**변형**

- **Insight Card** (홈 인사이트, 가로 스와이프): 너비 280, `--paper` 배경, 좌측 4px 컬러 스트라이프 (insight 종류별)
- **Widget Card** (홈 Block 2, 세그먼트별 우선): 풀 너비, `--paper` 배경 + 머스타드 1px 보더
- **Transaction Card** (분석 탭 거래): 컴팩트, 64h, 좌측 카테고리 아이콘
- **Story Card** (월간 리포트): 매거진 스타일, Gowun Batang 제목 + 큰 인용부호
- **Goal Card** (목표 탭): 진행률 게이지 + 잔여 액션

### 3. 차트 (Chart)

#### 3-1. 도넛 차트 (지출 분류)
- 외곽 24, 내경 12 비율
- 무채색 그라데이션 (`#2E3F30` → `#8B8478`)으로 카테고리 구분 — **컬러 의미 부여 X**
- 중앙: 총 지출 + 단위
- 호버/탭 시: 해당 섹터 +4px 확장, 카테고리명 표시

#### 3-2. 막대 차트 (월별 비교)
- 막대 두께 16, 간격 12
- 평균선: `--mustard` 점선 1px
- 세그먼트 평균선: `--terracotta` 점선 1px (벤치마크 비교)
- 음수 막대: `--negative` (테라코타 계열)

#### 3-3. 라인 차트 (자산 추이)
- 1.5px 두께, `--forest`
- 영역 채우기: `--forest`의 0.08 알파
- 마커는 호버/탭 시에만

#### 3-4. 게이지 (재무 건강도, 또래 대비)
- 반원형, 시계방향 0~100 스코어
- 호: 1.5px `--faint` 트랙 + `--terracotta` 채움
- 정점 숫자: `--t-num-xl`
- 하단 라벨: `또래 평균 72점` (벤치마크 표시)

### 4. 입력 (Input)

- 높이 56 (은퇴 64), 좌측 패딩 16
- 보더 1.5px `--faint-2`, 포커스 시 `--forest`
- 라벨: 상단 외부 캡션, `--t-caption`
- 에러: 하단 작은 텍스트, `--negative`
- 금액 입력은 자동 천 단위 콤마

### 5. 채팅 UI (대화·핵심 컴포넌트)

#### 5-1. 사용자 버블
- 우측 정렬, 너비 max 80%
- 배경 `--forest`, 텍스트 `--cream`, `--r-5` (좌하 4px)
- 패딩 12·16

#### 5-2. AI 코치 버블
- 좌측 정렬, 너비 max 90%
- 배경 `--paper` + 1px `--faint`, 텍스트 `--forest`, `--r-5` (우하 4px)
- 패딩 16·20
- 좌측 상단에 작은 코치 아이콘 (32×32 원형, 테라코타 1px 보더)
- 응답 안에 거래 인용·차트 미니어처 인라인 가능

#### 5-3. 추천 질문 칩 (Suggested Prompts)
- 입력창 위 가로 스와이프
- Pill 형태, `--cream-deep` 배경, `--forest` 텍스트
- 세그먼트별 Top 5 (IA 문서 §상담탭 참고)

#### 5-4. 인용 (Citation Block)
AI가 거래·세그먼트 데이터 인용할 때:
```
┌──────────────────────────┐
│ ▎ 인용한 데이터           │  ← terracotta 좌측 바 3px
│ │ 4월 식비 423,000원      │
│ │ S06 평균 380,000원       │
└──────────────────────────┘
```

### 6. 칩·태그 (Chip / Tag)

- 카테고리 칩: `--cream-deep` 배경, `--r-pill`, `--t-caption`
- 세그먼트 라벨 칩(설정에서만 노출): `--mustard` 배경
- 상태 태그(초과/적정/여유): `--positive-bg`/`--negative-bg`/`--cream-deep` 배경

### 7. 탭바 & 네비게이션

```
┌──────────┬──────────┬──────────┬──────────┐
│   홈     │   분석   │   상담   │   목표   │
│   🏠     │   📊     │   💬     │   🎯     │
└──────────┴──────────┴──────────┴──────────┘
```
- 활성 탭: `--forest` 텍스트 + 아이콘, 하단 2px `--terracotta` 인디케이터
- 비활성: `--neutral`
- 배경: `--cream`에 상단 1px `--faint` 구분선
- 아이콘은 직접 디자인 (아래 §아이콘 시스템)

### 8. 모달·시트

- **Bottom Sheet**: `--r-4` 상단 모서리, 핸들 36×4 `--faint`, 배경 `--paper`
- **Alert Dialog**: 중앙, 너비 280, `--r-4`, Primary/Secondary 버튼 한 쌍
- **Toast**: 하단 24 마진, 56h, `--forest` 배경 / `--cream` 텍스트, 3초 자동 사라짐

### 9. 스코어 카드 (재무 건강도)

```
┌──────────────────────────────┐
│ 우리 가족 재무 건강도          │  ← eyebrow
│                              │
│        78점                   │  ← --t-num-xl
│ ─────●──────────              │  ← 게이지 바
│                              │
│ "S06 평균 72점보다 6점 높아요"│  ← 해석 narrative
│                              │
│ [어떻게 해석하나요? →]         │
└──────────────────────────────┘
```

---

## 🎭 아이콘 시스템

### 스타일
- **선 두께**: 1.75px (가는 펜)
- **모서리**: 둥근 끝 (round cap)
- **그리드**: 24×24 베이스, 20×20 옵티컬
- **컬러**: 항상 단색 (`currentColor`)
- **금기**: 그림자, 그라데이션, 컬러 fill

### 카테고리 아이콘 (18종)
식비 / 카페 / 교통 / 쇼핑 / 의료 / 교육 / 통신 / 주거(공과금) / 보험 / 구독 / 여가·문화 / 여행 / 자녀양육 / 부모부양 / 자기계발 / 경조사 / 세금·금융 / 기타

각 카테고리는 직선적·기하학 모티프 — 이모지·캐릭터 회피.

### 시스템 아이콘
홈·분석·상담·목표 / 알림 / 프로필 / 설정 / 검색 / 필터 / 더보기 / 닫기 / 뒤로 / 공유 / 다운로드 / 카메라 / 잠금

---

## 🌗 모드 시스템

### 1. 표시 모드 (3종)

| 모드 | 자동 트리거 | 변화 |
|---|---|---|
| **일반** | 기본 | 위 사양 그대로 |
| **은퇴 모드** | L5-L6 세그먼트 (S14, S15, S17, S18) | 폰트 +20%, 패딩 -8, 컴포넌트 단순화, 차트 1개로 제한, 아이콘 24→28 |
| **프리랜서 모드** | I3 세그먼트 (S04, S08) | 홈 첫 위젯이 "이번 달 입금 vs 고정비", 월간 → 분기/연간 토글 기본값 |

설정에서 수동 전환 가능.

### 2. 라이트/다크
- 시스템 설정 따름이 기본
- 설정에서 강제 가능
- 컬러는 §컬러시스템 다크 모드 표 참고

---

## ♿ 접근성 (Accessibility)

### 1. 콘트라스트 (WCAG AA 이상)

- 본문 텍스트 (`--forest` on `--cream`): 콘트라스트 비 **11.8:1** (AAA 통과)
- 보조 텍스트 (`--forest-soft` on `--cream`): **8.4:1**
- 강조 색상 (`--terracotta` on `--cream`): **4.6:1** — AA만 통과, **본문 사용 금지**, 버튼·아이콘만
- 대형 텍스트 (`--mustard` on `--cream`): 24px+ 또는 굵은 17px+ 에서만 사용

### 2. 터치 타겟
- 모든 인터랙티브 요소 최소 **44×44pt** (iOS) / **48×48dp** (Android)
- 은퇴 모드: 최소 **52×52pt**

### 3. 동적 타입
- iOS Dynamic Type / Android FontScale 시스템 설정 따름
- 최대 200%까지 깨지지 않게 검증

### 4. 스크린 리더
- 모든 차트는 시각적 + 텍스트 대안 (`aria-label`로 핵심 수치·해석 1문장)
- 게이지: "재무 건강도 78점, 또래 평균보다 6점 높음"
- 거래 카드: "4월 5일, 스타벅스, 6,500원, 카페 카테고리"

### 5. 색맹 안전
- **색상 단독으로 의미 전달 금지** — 양수/음수는 부호와 위치로도 구분
- 게이지/차트는 패턴·텍스처(점선·실선) 보조

### 6. 음성 입력 (Phase 2)
AI 상담 탭에서 음성 → 텍스트 받아쓰기 (은퇴 세대·바쁜 부모용). 네이티브 STT 활용.

---

## 🎚️ 세그먼트별 변주 가이드 (디자인 차원)

### 변주 가능한 6가지 디멘션

| 디멘션 | 일반 | 은퇴 (L5-L6) | 변동 수입 (I3) | 영유아 가구 (H3a) |
|---|---|---|---|---|
| **타입 스케일** | 기본 | +20% | 기본 | 기본 |
| **첫 화면 위젯** | 또래 비교 | 자산 잔존 시뮬 | 입금 vs 고정비 | 가족 건강도 |
| **차트 밀도** | 표준 | 1개로 제한 | 분기·연간 토글 우선 | 표준 |
| **인사이트 톤** | 직설·짧게 | 따뜻·천천히 | 안심 우선 | 가족 단위 |
| **추천 질문 Top5** | 일반 | 인출·연금 중심 | 변동 대응 중심 | 교육비·저축 중심 |
| **모션 강도** | 기본 | 50% (느리게) | 기본 | 기본 |

### 변주 금기
- **컬러 팔레트 자체는 절대 변경 X** — 모든 세그먼트가 같은 시각 정체성을 공유
- **레이아웃 구조 자체는 변경 X** — 4탭, 홈 4블록은 고정. 안에 들어가는 콘텐츠만 분기

---

## 🧪 디자인 토큰 (CSS 변수 통합)

```css
:root {
  /* Color */
  --cream: #F7F3EC;
  --cream-deep: #EFE8DA;
  --paper: #FAF6EE;
  --forest: #1C2A1E;
  --forest-soft: #2E3F30;
  --terracotta: #C66A4A;
  --terracotta-deep: #A85536;
  --mustard: #D4AF6A;
  --mustard-deep: #B8934B;
  --ink: #14201B;
  --highlight: #F0E5CE;
  --faint: rgba(28, 42, 30, 0.12);
  --faint-2: rgba(28, 42, 30, 0.24);

  /* Semantic */
  --positive: #5C7A4F;
  --positive-bg: #E8EFE0;
  --negative: #A8543E;
  --negative-bg: #F4E1D8;
  --neutral: #8B8478;
  --info: #5B6B7A;

  /* Type */
  --serif-kr: "Gowun Batang", "Noto Serif KR", serif;
  --serif-kr-alt: "Noto Serif KR", serif;
  --sans-kr: "Pretendard Variable", Pretendard, system-ui, sans-serif;
  --serif-en: "Fraunces", "Gowun Batang", serif;
  --mono: "JetBrains Mono", ui-monospace, monospace;

  /* Spacing (8pt) */
  --s-1: 4px; --s-2: 8px; --s-3: 12px; --s-4: 16px;
  --s-5: 24px; --s-6: 32px; --s-7: 48px; --s-8: 64px; --s-9: 96px;

  /* Radius */
  --r-1: 4px; --r-2: 8px; --r-3: 12px; --r-4: 16px;
  --r-5: 24px; --r-pill: 999px;

  /* Motion */
  --ease-out-paper: cubic-bezier(0.2, 0.8, 0.2, 1);
  --ease-in-paper: cubic-bezier(0.6, 0, 0.8, 0.2);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
  --d-quick: 120ms; --d-base: 240ms; --d-slow: 360ms;
  --d-narrative: 600ms; --d-gauge: 900ms;
}

/* Retirement mode override */
[data-mode="retirement"] {
  --t-body: 18px;
  --s-5: 16px;
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce) {
  --d-quick: 0ms; --d-base: 0ms; --d-slow: 0ms;
  --d-narrative: 0ms; --d-gauge: 0ms;
}
```

---

## ✅ 컴포넌트 체크리스트 (5단계 와이어프레임 입력)

이 시스템에서 정의된 빌딩 블록 — 5단계는 이 컴포넌트들을 조합하여 화면을 구성한다.

- [x] 버튼 (5종)
- [x] 카드 (5종)
- [x] 차트 (4종)
- [x] 입력
- [x] 채팅 UI (4종)
- [x] 칩·태그 (3종)
- [x] 탭바·네비게이션
- [x] 모달·시트 (3종)
- [x] 스코어 카드
- [x] 아이콘 (카테고리 18 + 시스템 14)
- [x] 모드 시스템 (3종)
- [x] 디자인 토큰 (CSS 변수)

---

## 🚀 다음 단계로의 입력

**5단계 와이어프레임에서 그릴 핵심 화면 (IA 문서 §전체 정보 구조도 기준)**

1. 온보딩 6단계 (웰컴·진단·성향·목표·연결·투어)
2. 홈 (S01·S04·S06·S09·S17 5종 변주)
3. 분석 탭 (지출·수입·자산·구독·예산)
4. 상담 탭 (대화 진입·진행·인용·추천 질문)
5. 목표 탭 (리스트·추가·달성)
6. 리포트 (주간·월간·계절)
7. 가족 공유 (초대·공동 대시보드)
8. 설정·더보기

각 화면은 위 컴포넌트 카탈로그만으로 구성 가능해야 한다 — 새 컴포넌트가 필요하면 이 문서에 먼저 추가.

---

*디자인 시스템 v1.0 — 5단계 와이어프레임 진행하면서 부족한 컴포넌트는 추가 정의*
