# AssetCoach 기술 아키텍처 (Technical Architecture)

> **단계**: 6단계 — 기술 아키텍처 사양
> **작성일**: 2026-05-04
> **입력 산출물**: 1~5단계 전부 — 특히 IA(`AssetCoach_information_architecture.md`), 디자인 시스템(`AssetCoach_design_system.md`), 와이어프레임(`AssetCoach_wireframes.md`)
> **목적**: Gemma 4 온디바이스 통합 사양 + 모바일 앱 빌드 가능한 수준의 기술 청사진

---

## 🎯 아키텍처 원칙

1. **온디바이스 우선** — 사용자 거래 데이터는 기기를 떠나지 않는다. 클라우드는 옵트인 보조에 한정.
2. **하이브리드 분류** — 명확한 거래는 규칙 엔진, 모호한 거래만 LLM. 비용·지연 최소화.
3. **세그먼트는 정적, 콘텐츠는 동적** — 18개 세그먼트는 결정 트리로 즉시 판정. AI 호출은 그 위의 콘텐츠 생성에만.
4. **응답 시간 예산 명시** — 모든 화면은 "어떤 데이터가 언제까지 와야 하는지" 분명히 한다. 사용자가 대기하는 동안의 표시는 디자인 시스템 컴포넌트로.
5. **모델은 OTA, 앱은 스토어** — 모델 가중치는 별도 채널 (앱 사이즈 제약·민감도 분리).
6. **장애 시 점진적 저하** — 모델 로드 실패해도 앱은 작동한다 (인사이트 카드만 빈다).

---

# 1. 시스템 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────┐
│                      모바일 앱 (iOS / Android)                │
│                                                             │
│  ┌────────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │  UI Layer  │  │  도메인 레이어   │  │  AI Layer (온)    │    │
│  │ (네이티브)  │  │              │  │                  │    │
│  │            │  │ 세그먼트 엔진   │  │ Gemma 4 E4B INT4│    │
│  │ SwiftUI    │  │ 인사이트 큐레터 │  │ 프롬프트 파이프라인│    │
│  │ Compose    │←→│ 거래 분류기    │←→│ 캐시 매니저       │    │
│  │            │  │ 중복 감지기    │  │ 응답 스트리밍     │    │
│  │            │  │ 목표 트래커    │  │                  │    │
│  └────────────┘  └──────────────┘  └──────────────────┘    │
│         ↕                ↕                  ↕              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │             데이터 레이어 (SQLCipher)                │   │
│  │  거래 · 계정 · 카테고리 · 세그먼트 · 목표 · 대화 ·    │   │
│  │  캐시 · 모델 가중치 메타                            │   │
│  └─────────────────────────────────────────────────────┘   │
│         ↕                ↕                                  │
│  ┌──────────────┐  ┌──────────────────────────────┐        │
│  │ CSV 파서      │  │ SMS 파서 (Android)            │        │
│  │ 카드 4·은행 3 │  │ Notification Listener         │        │
│  └──────────────┘  └──────────────────────────────┘        │
└─────────────────────────────────────────────────────────────┘
                              ↕ (옵트인 only)
┌─────────────────────────────────────────────────────────────┐
│         하이브리드 클라우드 (선택, Phase 1.5+)                  │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ 모델 OTA      │  │ 마스킹 리포트  │  │ Anonymous Telemetry│ │
│  │ (CDN, 해시검증) │  │ 생성 (옵트인)  │  │ (크래시·성능)      │ │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

# 2. 기술 스택 결정 표

| 레이어 | 선택 | 대안 (기각 사유) |
|---|---|---|
| **iOS UI** | SwiftUI 5 + Swift 6 | UIKit (생산성·디자인 시스템 매핑 비용 높음) |
| **Android UI** | Jetpack Compose + Kotlin 2.0 | XML/View (유지보수성·테마 매핑 비용) |
| **공유 코어** | Kotlin Multiplatform (KMP) — 도메인·데이터 레이어 | Rust(러닝커브 +), Flutter(네이티브 ML 통합 약함) |
| **AI 추론** | iOS: **MediaPipe LLM Inference** + Core ML 보조 / Android: **MediaPipe LLM Inference** | llama.cpp (모바일 양자화·최적화 부족), MLC LLM (배포 도구 부족) |
| **모델** | **Gemma 4 E4B**, INT4 양자화 (≈2.5GB) | Llama 3.2 3B (한국어 약함), Phi-3.5 (라이선스), 더 작은 모델(품질 미흡) |
| **로컬 DB** | SQLite + **SQLCipher** | Realm(번들 사이즈), Room/CoreData 직접 (암호화 보강 필요) |
| **CSV 파싱** | KMP 공유 (`commons-csv`/Kotlin native) | 플랫폼별 분기 (중복) |
| **SMS 파서 (Android)** | Notification Listener Service + 정규식 + 폴백 LLM | Telephony SmsReceiver (정책상 제한·OEM 차이) |
| **결제 시스템** | App Store In-App + Google Play Billing | Stripe(앱스토어 정책 위반) |
| **백업** | iOS: **CloudKit + iCloud Keychain** / Android: **Google Drive + Tink** | 자체 서버(프라이버시 약속과 충돌) |
| **빌드/CI** | iOS: Xcode Cloud / Android: GitHub Actions + Gradle | Bitrise(비용) |
| **분석** | 앱 자체 익명 텔레메트리 (옵트인) | Firebase(데이터 외부 흐름) |

---

# 3. AI 레이어 — Gemma 4 통합

## 3.1 모델 선택과 양자화

| 항목 | 사양 |
|---|---|
| **모델** | Gemma 4 E4B (Effective 4B 파라미터, 한국어 학습 강화) |
| **라이선스** | Apache 2.0 (상용·재배포 자유) |
| **양자화** | **INT4 (Q4_K_M)** — 품질·메모리 균형점 |
| **메모리 풋프린트** | 가중치 ≈2.5GB / KV 캐시 ≈300MB / 워킹 셋 ≈3.2GB 피크 |
| **백업 양자화** | INT8 (Q8_0, ≈4.5GB) — 8GB+ RAM 고급 기기에서 옵션 제공 |
| **최소 디바이스** | iPhone 14+ (6GB), Galaxy S22+ (8GB) — 그 이하는 클라우드 폴백 안내 |

### 다운로드·검증

#### 사용자 동의 흐름
- 온보딩 §1.7 "AI 코치 준비" 화면에서 **명시적 동의** 후 다운로드 시작 (앱 번들에 포함하지 않음 — App Store/Play Store 사이즈 제약)
- 다운로드 위치: 앱 전용 보안 디렉토리 (sandboxed)
- 동의 화면 표시 항목: 모델 사이즈 / 예상 시간 / 사용 가능 디스크 공간 / "왜 이렇게 큰지" 설명
- 거부 시: **샘플 모드**로 진입 (모델 없이 거래·통계 작동, AI 의존 기능은 placeholder 표시 — 와이어프레임 §1.7 케이스별 변주)

#### 사전 체크
| 검사 | 임계 | 미달 시 동작 |
|---|---|---|
| 디스크 가용 공간 | 모델 사이즈 + 1GB 마진 (총 3.5GB) | Primary 비활성, "공간 정리 가이드" Tertiary 링크 노출 |
| 네트워크 종류 | Wi-Fi 권장 | 셀룰러 시 추가 확인 모달 ("약 2.5 GB의 데이터를 사용합니다") |
| RAM 클래스 | 6GB 이상 (B 클래스) | C 클래스(< 6GB)는 클라우드 폴백 안내 (Phase 1.5+) |
| 배터리 | 30% 이상 또는 충전 중 | 미달 시 경고만 노출, 진행은 사용자 선택 |

#### 다운로드 메커니즘
| 항목 | iOS | Android |
|---|---|---|
| 백그라운드 다운로드 | `URLSession` background configuration | `WorkManager` + Foreground Service (Notification 표시) |
| 재개 가능 | 자동 (HTTP Range 요청) | 자동 (HTTP Range 요청, OkHttp Interceptor) |
| 셀룰러 정책 | `URLSessionConfiguration.allowsCellularAccess` | `NetworkType.UNMETERED` 우선 (사용자가 셀룰러 동의 시 `CONNECTED`) |
| 충전 조건 | 옵션 (사용자 설정) | 옵션 (`Constraints.requiresCharging`) |
| 진행 보고 | `URLSessionDownloadDelegate` → 앱 내 옵저버블 | `WorkInfo.progress` LiveData |
| 분할 다운로드 | 단일 파일 (현 사양) | 단일 파일 (현 사양) — Phase 2에서 chunk 분할 검토 |

#### 무결성 검증
1. 다운로드 완료 시 SHA-256 해시 계산
2. 메타데이터 엔드포인트의 기대 해시와 비교
3. **일치**: 보안 디렉토리로 이동, `model_meta` 테이블에 `version`·`sha256`·`size_bytes`·`downloaded_at` 기록
4. **불일치**: 파일 즉시 삭제, 자동 재시도 1회
5. 재시도 실패: CDN 미러 (보조 호스트) 시도
6. 미러도 실패: 사용자 알림 (Toast + 재시도 버튼) + 진단 텔레메트리(옵트인 시)에 실패 보고

#### 진행 상태 UI
- §1.7-B 다운로드 진행 화면에 `gauge-linear` 컴포넌트로 표시 (디자인 시스템 §컴포넌트)
- 백그라운드 전환 시: 시스템 알림 ("AI 코치 받는 중 56% · 남은 시간 약 3분")
- 완료 시: 푸시 알림 ("AI 코치 준비 완료") + 첫 투어 진입 가능 상태 토글

#### CDN 인프라
- 주: AWS CloudFront 또는 GCP Cloud CDN (한국 서버)
- 미러: 일본·싱가포르 보조 호스트
- 모델 메타데이터 (버전·해시·릴리스 노트)는 별도 저용량 엔드포인트 (`<1KB JSON`)
- 사용자 1인당 ~2.5GB → Phase 1 100K 사용자 누적 약 250TB 트래픽 — 비용 견적·예약 트래픽 협상 필요 (M9 알파 직전)

#### 모델 업데이트
| 변경 종류 | 사용자 동의 | 네트워크 | 기존 모델 |
|---|---|---|---|
| 메이저 (e.g., gemma4 → gemma5) | 명시적 동의 | Wi-Fi 강제 | 검증 후 교체 |
| 마이너 (e.g., 1.2 → 1.3, 품질 개선) | 백그라운드 자동 통지 | Wi-Fi 권장 | 보존 후 교체 |
| 패치 (e.g., 1.2.1 → 1.2.2, 안전 가드) | 자동 | 모든 네트워크 | 즉시 교체 |
| **롤백** | 시스템 트리거 (응답 실패율 임계 초과) | 모든 네트워크 | 직전 버전 메타데이터로 자동 복원 |

#### 모델 없이 작동하는 기능 (샘플 모드 / 다운로드 거부 시)
| 기능 | 가용 |
|---|---|
| 거래 임포트·분류 | ✅ (규칙 엔진만 — §7) |
| 통계·차트 표시 | ✅ |
| 목표·예산 관리 | ✅ |
| 구독·중복 추적 | ✅ |
| AI 상담 (A2) | ❌ placeholder ("AI 코치 준비 중 — 설정에서 다운로드") |
| 인사이트 카드 (A1) | ❌ 통계 요약 카드만 표시 |
| 매거진 리포트 (A3) | ❌ 표 요약으로 대체 |
| 추천 질문 (A5) | ❌ 정적 fallback 5개 |
| 카테고리 분류 LLM 폴백 (A4) | ❌ "기타"로 분류 후 사용자 확인 요청 |

설정 → "AI 코치 추가하기"에서 **언제든 다운로드 시작** 가능. 모델 도착 즉시 ✅로 활성화.

## 3.2 추론 엔진

```
┌─────────────────────────────────────────────────┐
│           Inference Manager (KMP)                │
│                                                 │
│   요청 큐 ──→ 우선순위 스케줄러 ──→ 응답 스트림    │
│                       │                         │
└───────────────────────┼─────────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
   iOS 어댑터                       Android 어댑터
   MediaPipe LLM                  MediaPipe LLM
   + Metal 가속                    + GPU/NNAPI 가속
```

### 우선순위 스케줄러
- **P0 (실시간)**: 상담 응답 — 사용자가 화면 응시 중. 다른 작업 모두 양보
- **P1 (전경)**: 카테고리 분류 (LLM 폴백) — 새 거래 입력 시 인라인
- **P2 (지연 가능)**: 홈 인사이트 — 백그라운드, 5초 이내 도착
- **P3 (배치)**: 주간 리포트 — 충전 + Wi-Fi + 화면 꺼짐 시에만

P0 발생 시 P2/P3는 즉시 일시정지·재개. 한 번에 한 추론만 (모바일 GPU 동시성 제약).

## 3.3 5개 AI 호출 지점 매트릭스

| # | 호출 지점 | 우선순위 | 컨텍스트 길이 | 출력 길이 | 응답 시간 목표 | 캐싱 전략 | 트리거 |
|---|---|---|---|---|---|---|---|
| **A1** | 홈 인사이트 카드 | P2 | ≤ 1.5K tok (월 통계 요약) | ~80 tok × 3장 | 화면 진입 후 ≤ 5초 | 일 1회 batch + 데이터 변경 무효화 | 앱 진입 / 새 거래 ≥ 5건 누적 |
| **A2** | 상담 응답 | P0 | ≤ 4K tok (대화 히스토리 + 인용 거래) | ~150~400 tok 스트리밍 | TTFT ≤ 1.5초, TPS ≥ 20 | 거의 캐싱 안 됨 (의도적) | 사용자 메시지 전송 |
| **A3** | 주간 리포트 | P3 | ≤ 6K tok (주간 거래 + 세그먼트 벤치마크) | ~600~900 tok | 백그라운드 ≤ 90초 | 주 1회 결과 캐싱 | 일요일 23:00 (디바이스 충전·Wi-Fi 시) |
| **A4** | 카테고리 분류 (LLM 폴백) | P1 | ≤ 200 tok (가맹점명·금액·메모) | 1 tok (카테고리 ID) | ≤ 300ms | 가맹점명 → 카테고리 영구 캐시 | 규칙 엔진 신뢰도 < 0.7일 때만 |
| **A5** | 추천 질문 생성 | P3 | ≤ 1K tok (세그먼트 + 시즌 + 최근 변화) | ~30 tok × 5개 | 백그라운드 | 세그먼트 × 시즌 × 큰변화-시그니처 → 영구 캐시 (히트 95%+ 목표) | 세그먼트 변화 / 매주 |

### 컨텍스트 길이 산출 근거
- A2 4K: 대화 최근 12턴(~2K) + 인용 거래 30건 요약(~1K) + 시스템 프롬프트(~500) + 여유
- A3 6K: 주간 거래 ~150건 카테고리·금액·메모 압축(~3.5K) + 세그먼트 벤치마크(~1K) + 시스템·서식(~1.5K)

## 3.4 프롬프트 파이프라인

모든 호출은 동일 파이프라인을 통과한다:

```
[Raw Query] → [Segment Injector] → [Context Composer] → [Safety Guard]
            → [Token Trimmer] → [Gemma 4] → [Output Validator]
            → [Citation Linker] → [Cache Updater] → [UI Renderer]
```

### Segment Injector
사용자 세그먼트(S06 등) + 성향 3축 + 표시 모드를 시스템 프롬프트에 주입. 세그먼트 매트릭스 문서의 **세그먼트별 시스템 프롬프트 템플릿** 사용.

```
당신은 AssetCoach의 AI 코치입니다.
지금 대화하는 사용자는: S06 영유아 가구 맞벌이 (L3 / I1 / H3a)
사용자 성향: 소비형 4점, 알림 보통, 위험 중간
표시 모드: 일반

이 사용자에게 답할 때 우선순위:
1. 가족 단위로 말하기 ("우리 가족", "두 분이서")
2. 교육비·자녀 양육비 맥락 자연스럽게 인지
3. 절대 다른 가구 (싱글, 은퇴자) 기준으로 판단하지 않기
...
```

### Context Composer
호출 지점별로 거래·통계·세그먼트 벤치마크를 압축해서 동봉. 모든 거래 노출하지 않고 **"이번 달 변화가 큰 카테고리 상위 N개"**만 주입.

### Safety Guard
- 종목명·티커가 출력에 포함되면 **검열·재생성**
- 수익률 예측 표현(`연 X% 기대`)이 나오면 차단
- 의료·세무 단정 표현 차단 ("이건 환급 받으세요" → "전문가와 상담해 보세요")

### Output Validator
- JSON 모드 호출(A1, A4, A5)은 스키마 검증 후 통과
- 자연어 호출(A2, A3)은 길이·금기 표현 후처리

### Citation Linker
A2 응답에 거래 ID 참조가 등장하면 → 실제 DB 거래에 매핑하여 채팅 UI의 Citation Block(디자인 시스템 §채팅 UI)으로 렌더.

## 3.5 캐싱 전략

| 캐시 종류 | 키 | TTL | 무효화 트리거 | 예상 히트율 |
|---|---|---|---|---|
| **카테고리 분류** | `(가맹점명 정규화 + 금액대 + 메모 해시)` | 영구 | 사용자 수동 재분류 | 92~96% |
| **추천 질문** | `(세그먼트 ID + 시즌 + 큰 변화 시그니처)` | 7일 | 세그먼트 변경 / 큰 변화 발생 | 85% |
| **인사이트 카드** | `(월 + 세그먼트 + 통계 시그니처)` | 1일 | 새 거래 5건+ / 월 변경 | 60% |
| **주간 리포트** | `(주차 + 세그먼트 + 데이터 시그니처)` | 영구 | 데이터 시그니처 변화 | 99% (주1회 1번만) |
| **시스템 프롬프트** | `(세그먼트 ID)` | 앱 버전 | 앱 업데이트 | 100% |

KV 캐시 (Gemma 4 attention 캐시)는 **세션 단위로만** — 상담 한 대화 내에서 직전 턴까지 재사용, 세션 종료 시 폐기.

---

# 4. 세그먼트 엔진

## 4.1 결정 트리 (정적)

온보딩 4질문 입력 → 즉시 18개 중 하나로 매핑. AI 호출 없음.

```
function classifySegment(birth, income, household, childAge):
  age = currentYear - birth
  L = mapAgeToLifeStage(age)  // L1~L6
  I = income                   // I1~I6
  H = mapHousehold(household, childAge)  // H1, H2, H3a-d, H4
  return SEGMENT_LOOKUP[L][I][H]  // → S01~S18 또는 가장 가까운
```

`SEGMENT_LOOKUP` 테이블은 세그먼트 매트릭스 문서의 18개 정의 + fallback 규칙 (`(L, I, H)` 조합이 표에 없으면 가장 가까운 세그먼트로 흡수).

## 4.2 성향 3축 (동적)

| 축 | 점수 범위 | 데이터 소스 |
|---|---|---|
| **소비 성향** | 1~5 | 온보딩 Q + 행동: 월 평균 외식·쇼핑 비중, 충동 결제 빈도 |
| **참여 성향** | 1~5 | 알림 응답률, 상담 탭 진입 빈도, 목표 수정 빈도 |
| **위험 성향** | 1~5 | 온보딩 Q + 행동: 비상금/총자산 비율, 투자 자산 비중(자가 입력 시) |

3개월 데이터 누적 후부터 행동 보정 시작. 그 전까지는 온보딩 답변만 사용.

## 4.3 세그먼트 변경 감지

다음 이벤트에서 **재분류 제안**:
- 자녀 연령 임계 (영유아→초중→고대→독립)
- 수입 형태 변화 (월급→프리랜서 패턴 6개월 지속 시)
- 은퇴 트리거 (연금 입금 패턴 3개월 + 정기 수입 사라짐)

자동 변경 X — 항상 **사용자 확인 후** 적용. 변경 시 홈 화면이 새 세그먼트로 재구성된다는 미리보기 제공.

---

# 5. 데이터 레이어

## 5.1 스키마 (SQLite + SQLCipher)

```sql
-- 모든 테이블 SQLCipher 암호화 (key는 디바이스 keystore에서 도출)

CREATE TABLE accounts (
  id TEXT PRIMARY KEY, name TEXT, type TEXT,
  institution TEXT, last_synced INTEGER
);

CREATE TABLE transactions (
  id TEXT PRIMARY KEY, account_id TEXT, ts INTEGER,
  amount INTEGER,            -- 원 단위 정수, 음수=지출
  raw_text TEXT,             -- 가맹점 원문
  merchant_norm TEXT,        -- 정규화된 가맹점명
  category_id INTEGER,
  category_confidence REAL,  -- 분류 신뢰도
  category_source TEXT,      -- 'rule' | 'llm' | 'user'
  is_duplicate INTEGER DEFAULT 0,
  duplicate_group_id TEXT,
  notes TEXT,
  INDEX (ts), INDEX (account_id, ts), INDEX (category_id)
);

CREATE TABLE categories ( id INTEGER PRIMARY KEY, name TEXT, parent_id INTEGER );

CREATE TABLE user_profile (
  id INTEGER PRIMARY KEY DEFAULT 1,
  name TEXT, birth_year INTEGER,
  segment_id TEXT,           -- 'S06'
  income_pattern TEXT,       -- 'I1'
  household TEXT,            -- 'H3a'
  child_ages TEXT,           -- JSON array
  display_mode TEXT,         -- 'normal' | 'retirement' | 'freelance'
  consumption_score REAL,
  engagement_score REAL,
  risk_score REAL,
  updated_at INTEGER
);

CREATE TABLE goals (
  id TEXT PRIMARY KEY, title TEXT, target_amount INTEGER,
  current_amount INTEGER, deadline INTEGER, created_at INTEGER,
  status TEXT, segment_recommended INTEGER
);

CREATE TABLE conversations (
  id TEXT PRIMARY KEY, started_at INTEGER, ended_at INTEGER,
  message_count INTEGER, summary TEXT
);

CREATE TABLE messages (
  id TEXT PRIMARY KEY, conversation_id TEXT, role TEXT,
  content TEXT, created_at INTEGER, citations_json TEXT
);

CREATE TABLE ai_cache (
  cache_key TEXT PRIMARY KEY, cache_type TEXT,
  payload TEXT, created_at INTEGER, expires_at INTEGER, hits INTEGER
);

CREATE TABLE model_meta (
  model_id TEXT PRIMARY KEY, version TEXT, sha256 TEXT,
  size_bytes INTEGER, downloaded_at INTEGER, last_used_at INTEGER
);
```

## 5.2 키 관리

| 플랫폼 | 키 저장 | DB 암호화 |
|---|---|---|
| iOS | Keychain (Secure Enclave 보호) | SQLCipher AES-256, 키는 사용자 unlock 후 메모리에만 |
| Android | Android Keystore (StrongBox 가능 시) | SQLCipher AES-256, BiometricPrompt로 복호화 unlock |

앱 잠금 옵션: 생체 인증 (Face ID / 지문) 또는 PIN. 백그라운드 5분 후 자동 잠금.

## 5.3 백업

- iOS: **CloudKit Private DB** — 사용자 iCloud 계정의 비공개 영역, end-to-end 암호화 (사용자 외 Apple도 못 읽음)
- Android: **Google Drive 앱 폴더** — 사용자 Drive 비공개 영역, AES-256 사전 암호화

백업 단위: 거래·목표·세그먼트 프로필. 모델 가중치는 백업하지 않음 (재다운로드).

---

# 6. 데이터 수집

## 6.1 CSV 파서

지원 포맷 7종 (Phase 1):

| 기관 | 포맷 | 컬럼 자동 매핑 |
|---|---|---|
| 신한카드 | 신한 표준 CSV | 거래일·가맹점·금액·승인번호 |
| 삼성카드 | 삼성 표준 CSV | 거래일·가맹점·승인금액 |
| 현대카드 | 현대 표준 CSV | 결제일·가맹점·금액 |
| KB국민카드 | KB 표준 CSV | 사용일·가맹점·이용금액 |
| 신한은행 | 거래내역 엑셀→CSV | 거래일·적요·출금·입금·잔액 |
| KB국민은행 | 거래내역 엑셀→CSV | 거래일·적요·출금·입금·잔액 |
| 우리은행 | 거래내역 엑셀→CSV | 거래일·적요·출금·입금·잔액 |

### 파서 로직
1. 인코딩 자동 감지 (CP949 / UTF-8)
2. 헤더 행 식별 → 포맷 분기
3. 행 단위 파싱 → 정규화된 `Transaction` 객체
4. **중복 체크** (동일 가맹점·금액·일자·기존 DB 매칭) → 임포트 스킵
5. 카테고리 분류 (§7) → 저장

### 사용자 검토
초기 임포트 시 분류 신뢰도 낮은 거래(< 0.7)는 사용자 확인 후 저장. 학습 데이터로 활용.

## 6.2 SMS 결제 알림 파서 (Android만)

```
[권한] BIND_NOTIFICATION_LISTENER_SERVICE
       (사용자 명시적 허용 + 시스템 설정 진입 가이드)

[감지] 카드사·은행 발신자 화이트리스트
       신한카드, 삼성카드, 현대카드, KB카드, 우리은행, 신한은행, ...

[파싱] 정규식 1차 → 매칭 실패 시 LLM 폴백 (A4 호출)
       예: "[Web발신]\n신한카드 승인 김XX님\n5,800원\n04/22 14:23\n스타벅스 종로"
       → { 카드: 신한, 금액: 5800, 일시: 2026-04-22T14:23, 가맹점: 스타벅스 종로 }

[저장] CSV 파서와 동일 흐름으로 통합
```

iOS는 시스템 정책상 SMS 자동 파싱 불가 → CSV 업로드 또는 Phase 3 마이데이터로 보완.

---

# 7. 카테고리 분류 (하이브리드)

```
┌──────────────────────────────────────────────────┐
│ 1. 가맹점명 정규화 (대소문자, 공백, 지점명 제거)     │
│    "스타벅스 종로점 SK B" → "스타벅스"             │
├──────────────────────────────────────────────────┤
│ 2. 정규화된 명 → 영구 캐시 조회                    │
│    히트(95%+): 즉시 카테고리 반환                 │
├──────────────────────────────────────────────────┤
│ 3. 규칙 엔진 (수천 개 매핑 + 키워드 룰)             │
│    "스타벅스" → 카페 / "GS25" → 식비 / "SKT" → 통신│
│    신뢰도 ≥ 0.7: 캐시에 기록 후 반환              │
├──────────────────────────────────────────────────┤
│ 4. LLM 폴백 (A4)                                  │
│    프롬프트: 가맹점·금액·메모 → 18개 카테고리 중 1│
│    JSON 응답 강제, schema 검증, 캐시 기록          │
├──────────────────────────────────────────────────┤
│ 5. 모두 실패: "기타" + 사용자 확인 요청            │
└──────────────────────────────────────────────────┘
```

### 18개 카테고리
디자인 시스템 §아이콘 카테고리 18종과 1:1 매핑. ID 1~18 정수.

### 사용자 재분류 학습
사용자가 카테고리를 수정하면 → `(merchant_norm + amount_band) → category` 영구 캐시에 덮어씀. 이후 같은 가맹점은 항상 사용자 선택 사용.

---

# 8. 중복 거래 감지

```
function detectDuplicates(newTx, existingTxs):
  candidates = existingTxs.filter(t =>
    abs(t.amount) == abs(newTx.amount) AND
    abs(t.ts - newTx.ts) < 7days AND
    similarity(t.merchant_norm, newTx.merchant_norm) > 0.85
  )
  if candidates.size > 0:
    return { is_duplicate: true, group: candidates[0].duplicate_group_id || newGroupId() }
```

또한 **구독 중복 패턴** (§3.4 인사이트 카드 예시):
- 동일 서비스 (Netflix 가족 + Netflix 개인) → 가맹점 매핑 테이블에서 같은 root로 묶이는 두 결제가 **같은 달에 동시 존재**할 때 알림 카드.

---

# 9. 보안·프라이버시

## 9.1 데이터 흐름 약속

| 데이터 | 위치 | 외부 전송 |
|---|---|---|
| 거래 원문·금액 | 디바이스 SQLCipher | **절대 없음** |
| 카테고리·통계 집계 | 디바이스 | 없음 (옵트인 마스킹 리포트 한정) |
| 사용자 입력 (대화) | 디바이스 | 없음 |
| 모델 가중치 | 디바이스 (서명 검증) | 첫 다운로드 시 CDN→디바이스 |
| 익명 텔레메트리 | 옵트인 시 | 크래시 스택·앱 버전·디바이스 모델만 |
| 백업 | iCloud / Drive (사용자 계정) | 사용자 계정 한정 + 사전 암호화 |

## 9.2 권한 정책
- SMS Notification Listener (Android): 명시적 동의·예시 화면·언제든 회수
- 알림: 표준 시스템 권한
- 카메라/사진: 영수증 OCR(Phase 1.5) 시점에만 요청
- 연락처/위치: **요청하지 않음**

## 9.3 법적 디스클레이머
화면 진입점·리포트·상담 응답에 표시:
> AssetCoach는 투자자문업자가 아니며, 본 정보는 일반적 참고용입니다. 종목·상품에 관한 의사결정은 자격을 갖춘 전문가와 상담하세요.

세그먼트 매트릭스 §법적 안전 기준의 4가지 금기 (종목 추천 / 수익률 예측 / 상품 권유 / 의료·세무 단정)는 §3.4 Safety Guard에서 강제.

---

# 10. 성능 예산

| 항목 | 목표 | 임계 (실패 정의) |
|---|---|---|
| 앱 콜드 스타트 → 홈 진입 | 1.5초 | > 3초 |
| 모델 콜드 로드 (앱 실행 후 첫 추론) | < 3초 | > 6초 |
| 상담 TTFT | < 1.5초 | > 3초 |
| 상담 TPS | > 20 tok/s | < 10 tok/s |
| 홈 인사이트 카드 (캐시 히트) | < 50ms | > 300ms |
| 홈 인사이트 카드 (캐시 미스, A1 호출) | < 5초 | > 10초 |
| 거래 임포트 1000건 | < 8초 | > 20초 |
| 메모리 피크 | < 4GB (모델 포함) | > 5GB |
| 1시간 적극 사용 배터리 | < 12% | > 20% |
| 앱 사이즈 (스토어) | < 80MB | > 150MB |
| 모델 사이즈 (별도 다운로드) | < 3GB | > 4GB |

### 디바이스 클래스
- **A 클래스** (iPhone 14+ Pro, S23+ Ultra, Pixel 8 Pro): 모든 목표 충족
- **B 클래스** (iPhone 13/14, S22, Pixel 7): TPS 15+, TTFT 2초 — 약간 완화
- **C 클래스** (iPhone 12, S21 등 6GB 미만): **클라우드 폴백 안내** + 앱 핵심 기능(거래·통계·차트)은 작동

---

# 11. 모바일 앱 스택 상세

## 11.1 모듈 구성

```
┌─────────────────────────────────────────┐
│  app-ios (SwiftUI)    app-android       │
│                       (Compose)          │
├─────────────────────────────────────────┤
│  shared (Kotlin Multiplatform)           │
│   ├─ domain                              │
│   │   ├─ segment-engine                  │
│   │   ├─ classifier (rule + llm)         │
│   │   ├─ duplicate-detector              │
│   │   ├─ goal-tracker                    │
│   │   └─ insight-curator                 │
│   ├─ data                                │
│   │   ├─ db (SQLDelight + SQLCipher)     │
│   │   ├─ csv-parser                      │
│   │   ├─ sms-parser (android-only impl)  │
│   │   └─ backup                          │
│   ├─ ai                                  │
│   │   ├─ inference-manager               │
│   │   ├─ prompt-pipeline                 │
│   │   ├─ cache-manager                   │
│   │   └─ schedulers                      │
│   └─ design-tokens (codegen)             │
└─────────────────────────────────────────┘
```

## 11.2 디자인 토큰 코드젠
디자인 시스템 §디자인 토큰의 CSS 변수 → KMP 코드젠으로:
- iOS: `Color.cream`, `Spacing.s5` Swift enum
- Android: `MaterialTheme.assetCoach.cream`, `dimensionResource(R.dimen.s5)`
- 단일 source of truth — 디자이너가 토큰 변경하면 양 플랫폼 즉시 동기화

## 11.3 화면 → 컴포넌트 → 토큰 매핑
와이어프레임 §화면 인벤토리 30화면 × 디자인 시스템 §컴포넌트 카탈로그 → 모든 화면이 카탈로그만으로 빌드 가능. 새 컴포넌트 필요 시 디자인 시스템 문서에 먼저 추가.

---

# 12. 하이브리드 클라우드 (선택)

Phase 1 출시 시점에는 **사용 안 함**. Phase 1.5에서 옵션으로 도입 검토.

## 12.1 마스킹 월간 리포트 (옵트인)
- 거래 원문 미전송 — 카테고리·금액·통계만
- 클라우드에서 더 큰 모델로 깊이 있는 매거진 리포트 생성
- 디바이스에서 받아 그대로 표시
- 사용자가 끄면 즉시 디바이스 추론으로 폴백

## 12.2 익명 텔레메트리
- 크래시 스택, 앱 버전, 디바이스 모델, 추론 지연 통계
- **사용자 ID·거래·세그먼트 정보는 포함하지 않음**
- 옵트인 (기본 OFF). 설정 → 정보 → 진단 데이터 보내기

## 12.3 모델 OTA
- 모델 가중치 업그레이드는 별도 CDN
- 메타데이터(버전·해시·지원 기기·릴리스 노트)만 백엔드 조회
- 사용자가 Wi-Fi 환경에서 수동 업데이트 (자동 업데이트는 옵션)

---

# 13. 배포·업데이트

## 13.1 릴리즈 채널
- **앱**: App Store / Google Play 표준
- **모델**: 자체 CDN, 앱 내 메타데이터 폴링 (앱 시작 + 주1회)

## 13.2 버전 매트릭스
| 컴포넌트 | 버전 정책 | 호환성 |
|---|---|---|
| 앱 | semver (예: 1.0.3) | 모델 메이저 버전 N, N-1 호환 |
| 모델 | 메이저.마이너 (예: gemma4-e4b-int4-1.2) | 앱 마이너 버전 매트릭스 사전 검증 |
| 프롬프트 | 별도 버저닝 (앱 번들에 포함) | 앱과 함께 배포 |

## 13.3 점진 배포
- Beta (TestFlight / Play Internal) → 5% 롤아웃 → 25% → 100%
- 크래시율 임계 0.5%, 모델 응답 실패율 임계 1% — 초과 시 자동 롤백 (관제 대시보드)

---

# 14. 테스트 전략

## 14.1 자동 테스트
| 종류 | 대상 | 도구 |
|---|---|---|
| 단위 테스트 | 도메인 로직 (세그먼트 분류, 중복 감지, 분류기 룰) | KMP `kotlin.test` |
| 골든 데이터 테스트 | CSV 파서 7종 (각 100+ 행 샘플 + 정답지) | 스냅샷 비교 |
| AI 출력 테스트 | A1·A4·A5 JSON 응답 스키마 | 100건 샘플 회귀 |
| 안전성 테스트 | A2·A3 금기 표현 회귀 (종목·수익률 등 50개 시나리오) | 매 모델 업데이트마다 |
| UI 회귀 | 핵심 화면 5종 × 5세그먼트 | iOS/Android 스크린샷 비교 |
| 성능 회귀 | §10 성능 예산 항목 전체 | 매 빌드 자동 측정 |

## 14.2 세그먼트 검증
- 18개 세그먼트 각각 **합성 거래 데이터셋** (3개월·100건+)
- 홈 화면이 세그먼트별 우선 위젯과 인사이트로 자동 구성되는지 검증
- A2 상담 응답이 세그먼트 톤·금기를 지키는지 회귀

## 14.3 기기 테스트
- A 클래스 3종, B 클래스 3종, C 클래스 2종 (총 8종) Lab Farm 회귀
- 각 빌드 야간 자동 실행

## 14.4 클로즈드 베타
- 5~6개월차 시작 — 18개 세그먼트별 5명씩 (90명)
- 주간 NPS·핵심 사용 지표
- 응답 품질 4점 이상(5점 만점) 80% 시 일반 출시

---

# 15. 마일스톤·일정 (개발 단계)

| 월 | 마일스톤 | 핵심 산출물 |
|---|---|---|
| **M1** | 인프라 부트스트랩 | KMP 모노레포, 디자인 토큰 코드젠, SQLCipher 통합, MediaPipe LLM 통합, Gemma 4 INT4 첫 추론 성공 |
| **M2** | 데이터 수집 | CSV 파서 7종, 카테고리 분류 룰 엔진 + 캐시, 중복 감지 |
| **M3** | 핵심 화면 | 온보딩 6단계, 홈 4블록 + S06 변주 1종, 분석 지출 |
| **M4** | 세그먼트 확장 | 18개 세그먼트 분기, 홈 변주 5종 (S01·S04·S06·S09·S17) 완성, 은퇴 모드 |
| **M5** | AI 파이프라인 | 5개 호출 지점(A1-A5) 구현, 프롬프트 v1, 캐시 매니저, 안전성 가드 |
| **M6** | 상담 탭 | 채팅 UI, 인용 블록, 추천 질문, 스트리밍 응답 |
| **M7** | 리포트·목표 | 주간·월간 리포트(A3), 다중 목표, 계절 인사이트 |
| **M8** | 가족 공유·구독 추적 | 배우자 초대 흐름, 구독 미사용·중복 감지 |
| **M9** | 알파 (내부) | 전 기능 통합, 회사 전 직원 50명 |
| **M10** | 클로즈드 베타 1 | 18 세그먼트 × 5명 = 90명, NPS 측정 |
| **M11** | 클로즈드 베타 2 + 안정화 | 응답 품질 회귀, 성능 예산 재검증, 크래시 < 0.3% |
| **M12** | 출시 | App Store / Google Play 일반 출시 |

총 약 **12개월** — `AssetCoach_handoff_v2.md` 9~12개월 일정 상한과 부합.

---

# 16. 리스크·미해결

## 16.1 기술 리스크

| 리스크 | 가능성 | 영향 | 완화 |
|---|---|---|---|
| Gemma 4 E4B 한국어 응답 품질 미흡 | 중 | 큰 | M1에서 50개 샘플 검증, 미흡 시 8B 양자화 옵션·또는 fine-tune 검토 |
| 모바일 추론 메모리 OOM | 중 | 큰 | INT4 + 컨텍스트 트리밍 + B/C 클래스 디바이스 클라우드 폴백 |
| 모델 다운로드 실패율 | 중 | 중 | CDN 다중 리전, 분할 다운로드, 재개 가능 |
| SMS 파서 OEM 호환성 | 높 | 중 | 카드사·은행 발신 패턴 100+ 케이스 데이터셋, LLM 폴백 |
| iOS App Store 심사 (AI·금융) | 중 | 큰 | 디스클레이머 명시, 투자 자문 표현 회피, 심사관 가이드 동봉 |

## 16.2 미해결 사항 (다음 단계로 이연)

- **마이데이터 라이선스** (Phase 3): 신청 절차·요건은 별도 법무 검토. 현재 설계는 라이선스 없이도 작동 (CSV·SMS).
- **투자 자문 영역** (Phase 4): 투자자문업 등록 후 검토. 현재 §법적 안전 기준에 따라 명시적 차단.
- **iOS SMS 자동 파싱**: 정책상 불가. CSV 보완 또는 Apple Wallet 거래 API 검토 (M11 시점에 재평가).
- **OCR 영수증 입력** (Phase 1.5): 디자인은 카메라 권한 가드만 두고 미구현.
- **다국어**: 한국어 단일. 영어는 Phase 2 이후.

---

# 17. 외부 인터페이스 요약

## 17.1 6단계 → 개발 인계
- 이 문서를 기준으로 **개발팀 헤드카운트 산정** 진행
- iOS 1, Android 1, KMP 코어 1, ML/AI 1, 디자이너 1, PM 1 — 최소 6명 풀타임 12개월
- 알파·베타 단계 QA 1명 추가 권장

## 17.2 디자이너 인계
- 디자인 시스템 토큰은 `shared/design-tokens` KMP 모듈에 코드로 동결
- 와이어프레임의 30화면이 빌드 단위 — 각 화면 별 컴포넌트 명세 sign-off 필요

## 17.3 PM 인계
- 18 세그먼트 검증 데이터셋 큐레이션이 M2 까지의 핵심 의존성
- 프롬프트 v1 작성·검증은 ML/AI 엔지니어 + PM 공동 작업
- 클로즈드 베타 모집·운영 (M9~M11) PM 주도

---

*v1.0 — Gemma 4 통합 및 12개월 개발 로드맵 정의. 알파 단계에서 모델 양자화 옵션·하이브리드 클라우드 도입 시점 재검토.*
